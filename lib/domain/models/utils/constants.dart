import 'package:flutter/material.dart';

class Constants {
  static BorderRadius borderRadius = BorderRadius.circular(20);
  static EdgeInsets padding = const EdgeInsets.all(12);
  static double buttonLength = 200;
}
